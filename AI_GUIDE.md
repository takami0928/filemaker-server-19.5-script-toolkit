# AI利用契約

このファイルは、Microsoft 365 Copilot等のAIが人間向けスクリプト設計書を生成するための指示書です。

中心成果物は、FileMaker担当者がScript Workspaceへ手作業で実装できる設計書です。XML、Script IR、clipboard出力は中心成果物ではありません。

## 対象

- FileMaker Server 19.5にホストされたカスタムApp
- FileMaker Pro 19.5での手作業実装
- client、PSOS、server schedule、WebDirect等の実行コンテキスト

## 必須処理順

1. 社内文書から対象システム情報を抽出する。
2. 確認済み事実、仮定、不足情報を分離する。
3. 必要なFileMakerオブジェクトを列挙する。
4. 実行コンテキストを選択する。
5. 各ステップのFileMaker 19.5互換性を確認する。
6. 入力JSONと結果JSONを定義する。
7. 正常系、エラー系、競合、再実行を設計する。
8. FileMaker形式のステップ一覧と各設定を出力する。
9. 未解決情報を明示する。
10. 人間向けテストケースを出力する。
11. FileMaker担当者がScript Workspaceへ手作業で実装する。
12. XMLは別途明示された場合だけ、実験的オプションとして扱う。

処理順を入れ替えて、未確認のオブジェクトや業務仕様を先に補完してはいけません。

## 社内文書から取得する情報

少なくとも次を、社内SharePoint等の対象システム資料から確認します。

- テーブル、テーブルオカレンス、フィールド
- レイアウトと基準TO
- 既存スクリプトと呼出元
- 業務フローと期待する副作用
- アカウント、アクセス権セット、必要な拡張アクセス権
- 命名規則
- 入力、結果、エラー時の業務要件
- 同時実行、再試行、重複実行に関する要件

社内情報は設計時の入力であり、この公開リポジトリや公開知識のSharePoint配布物へ保存しません。

## 設計状態と停止条件

### `draft design`

確認済み情報だけで作成できる範囲を設計します。仮定と不足情報を分離し、未解決箇所、選択肢、確認質問を明示します。

不足情報があっても設計全体を無条件に止める必要はありません。ただし、不明なオブジェクト名、内部ID、権限、業務仕様、入力値を推測で埋めてはいけません。

### `implementation-ready design`

FileMaker担当者がScript Workspaceへ手作業で実装できるだけの情報が揃った設計です。

少なくとも、必須オブジェクト、権限、実行コンテキスト、呼出元、入力契約、結果契約、各ステップの設定、エラー分岐が解決済みでなければ、`implementation-ready`と表現してはいけません。互換性の`partial`条件や`unknown`も解消が必要です。

`implementation-ready`は、XML生成、FileMakerへの貼り付け、runtime、FMSEの検証状態を意味しません。

## 必須出力

- スクリプト名
- 目的
- 実行コンテキスト
- 呼出元
- 前提条件
- 入力JSON
- 結果JSON
- 必要なレイアウト
- 必要なTO
- 必要なフィールド
- 必要な既存スクリプト
- 必要な権限
- 使用変数
- FileMaker形式のステップ一覧
- 各ステップの設定
- エラー分岐
- Commit／Revert
- ロック／競合
- 再試行／冪等性
- セキュリティ上の注意
- 未解決情報
- 人間向けテストケース
- 実機未検証事項

FileMaker形式のステップ一覧は、FileMaker担当者が上から順に実装できる粒度で、オプション、計算式、対象オブジェクト、分岐条件を併記します。

## 互換性判断順序

1. FileMaker Pro 19.5に候補ステップが存在するか確認する。
2. 対象実行コンテキストで`available`か確認する。`unavailable`は採用しない。
3. `partial`の条件または`unknown`の不足情報を解消する。どちらも単純な利用可能扱いにしない。
4. ステップ導入版とServer対応導入版を分け、19.5より後の機能を遡及していないか確認する。
5. 対象ファイル固有のレイアウト、TO、フィールド、スクリプト等が解決済みか確認する。
6. XMLが明示的に要求された場合だけ、互換性とは別にrenderer対応とfixture証拠を確認する。

`fms19 compat`または`fms19 list-steps`を使い、対象コンテキストごとに確認します。詳細は[FileMaker 19.5互換性カタログ](docs/COMPATIBILITY_CATALOG.md)を参照してください。

## 設計時の安全ルール

- サーバー処理がクライアントのレイアウト、対象レコード、変数、グローバルフィールドを引き継ぐと仮定しない。
- サーバー処理に必要な識別子とオプションは、明示した入力JSONで渡す。
- サーバー側で処理用レイアウトへ移動し、主キー等の確認済み識別子から対象を再検索する。
- 失敗し得る重要ステップの直後で`Get ( LastError )`を取得する。
- 正常系とすべてのエラー系で同じ構造の結果JSONを返す。
- 破壊的処理では対象範囲、権限、Commit／Revert、バックアップ・復旧方法を明示する。
- レコードロック、同時実行、有限回の再試行、再実行時の重複防止を設計する。

[5つの実用パターン](patterns/README.md)、[スクリプト作法](docs/SCRIPT_STYLE.md)、[サーバー実行設計](docs/SERVER_EXECUTION.md)を判断材料として使います。5パターンは`design_only`であり、対象要件のすべてを満たすとは限りません。

## XML／Script IR／clipboard

XMLは、別途明示された依頼と再承認がある場合だけ扱う凍結済みの実験的オプションです。

- 人間向け設計書の完成条件にXMLを含めない。
- 未対応ステップのXMLを推測生成しない。
- 未解決のFileMakerオブジェクト参照やblocking事項が残るScript IR v2からXMLを生成しない。
- XML生成、構造検査、FileMaker Pro 19.5貼り付け、client runtime、FMSE runtimeを別の状態として報告する。
- FileMaker Pro／Server 19.5実機未検証XMLを、正しいXML、検証済み成果物、正式成果物と表現しない。

## 設計優先順位

1. データを壊さない
2. FileMaker Server 19.5に適合させる
3. 入出力と実行コンテキストを明確にする
4. 上から読んで理解できるようにする
5. 変更しやすくする
6. テストしやすくする
7. 必要な範囲だけ再利用する
8. 不要な抽象化を避ける

## 禁止

- FileMaker 20以降の機能を19.5向けに使用すること
- 未提示のオブジェクト名、内部ID、権限、業務仕様を事実として捏造すること
- 現在のレイアウトや対象レコードを暗黙に利用するサーバースクリプト
- `With dialog: On`を前提にしたサーバー処理
- エラー取得前に別のステップを実行すること
- 対象を限定しない削除、全置換、一括更新
- 未解決情報がある設計を`implementation-ready`と表現すること
- 「設計済み」「XML生成済み」「実機検証済み」を同じ状態として扱うこと
- `design_only`パターンをXML生成可能または実機検証済みと表現すること
