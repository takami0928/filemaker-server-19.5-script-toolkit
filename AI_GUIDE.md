# AI利用契約

このファイルは、Microsoft 365 Copilot等のAIが最初に参照するための指示書です。

## 対象

- FileMaker Server 19.5にホストされたカスタムApp
- FileMaker Pro 19.5での開発
- FileMaker ServerのFMSE、Perform Script on Server、スケジュール

## 必須手順

1. 社内文書から、対象テーブル、フィールド、TO、レイアウト、既存スクリプト、権限、呼出元を抽出する。
2. 確認済み事実、仮定、不足情報を分ける。
3. `fms19 compat`または`fms19 list-steps`で、候補ステップのFileMaker 19.5互換性を対象コンテキストごとに確認する。
4. 実行場所を`client`、`psos`、`server_schedule`から明示する。
5. サーバー処理ではクライアントのレイアウト、対象レコード、変数、グローバルフィールドを引き継ぐと仮定しない。
6. サーバー処理に必要な識別子とオプションをJSONのスクリプト引数で渡す。
7. サーバー側で処理用レイアウトへ移動し、対象レコードを主キーから再検索する。
8. 重要なステップの直後で`Get ( LastError )`を取得する。
9. 結果はJSONで返す。
10. 破壊的処理は対象件数、主キー、権限、バックアップ・復旧方法を明示する。
11. 本リポジトリで検証済みのXMLテンプレートだけをJSONレンダラーで生成する。
12. 未対応ステップは、FileMaker Pro 19.5からコピーした実物XMLをフィクスチャとして取得するまで推測生成しない。
13. XML生成、FileMakerへの貼り付け、実行テストを別の状態として報告する。
14. Script IR v2を`schemas/script-ir.schema.json`で検証し、変数、コンテキスト、未解決事項、設計・証拠状態を省略しない。
15. FileMakerオブジェクトの内部IDが不明な場合は`resolution: "unresolved"`として保持し、推測値を設定しない。
16. 未解決オブジェクト参照またはblockingな未解決事項が残るネイティブv2からXMLを生成しない。

## 互換性判断順序

1. FileMaker Pro 19.5にステップが存在するか。
2. 対象コンテキストで`available`か。`unavailable`なら採用しない。
3. `partial`の条件または`unknown`の不足情報が残っていないか。どちらも単純な利用可能扱いにしない。
4. ステップ導入版とServer対応導入版を分け、19.5より後の機能を遡及していないか。
5. `catalog/fm19.5/verified-steps.json`由来のrenderer対応とfixture証拠があるか。互換性カタログだけでXML生成可能と判断しない。
6. 対象ファイル固有のレイアウト、TO、フィールド、スクリプト等のオブジェクト参照が解決済みか。

詳細とCLI例は[FileMaker 19.5互換性カタログ](docs/COMPATIBILITY_CATALOG.md)を参照してください。

## 設計優先順位

1. データを壊さない
2. FileMaker Server 19.5で動く
3. 入出力と実行コンテキストが明確
4. 上から読んで理解できる
5. 変更しやすい
6. テストしやすい
7. 必要な範囲だけ再利用する
8. 不要な抽象化を避ける

## 出力に必ず含めるもの

- 実行環境
- 前提と不足情報
- 変更対象
- スクリプト引数JSON
- スクリプト結果JSON
- 使用変数一覧
- スクリプトステップ
- 各重要処理のエラー確認
- サーバー実行時のコンテキスト再構築
- 正常系、0件、複数件、権限不足、レコードロック、再実行のテスト
- 貼り付け後に人間が確認する項目

## 禁止

- FileMaker 20以降の機能を19.5向けに使用すること
- 未提示のオブジェクトIDを事実として捏造すること
- 現在のレイアウトや対象レコードを暗黙に利用するサーバースクリプト
- `With dialog: On`を前提にしたサーバー処理
- エラー取得前に別のステップを実行すること
- 対象を限定しない削除・全置換・一括更新
- 「XML生成済み」を「実行確認済み」と表現すること
